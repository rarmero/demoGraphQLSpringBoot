type Author {
    id: ID!
    name: String!
    age: Int
}

type Query {
    authorById(id: ID): Author
    findAllAuthors: [Author]!
    countAuthors: Int!
}

type Mutation{
    addAuthor(name: String!, age: Int): Author!
}

